{
    "name": "datalog-mcp-server",
    "version": "1.0.0",
    "type": "module",
    "description": "MCP server for Datalog Studio integration",
    "main": "dist/index.js",
    "scripts": {
        "build": "bun i && tsc",
        "start": "bun run dist/index.js",
        "dev": "tsc --watch",
        "typecheck": "tsc --noEmit",
        "test": "echo \"Error: no test specified\" && exit 0"
    },
    "dependencies": {
        "@modelcontextprotocol/sdk": "^1.0.1",
        "axios": "^1.7.9",
        "form-data": "^4.0.1"
    },
    "devDependencies": {
        "@types/node": "^22.0.0",
        "typescript": "^5.0.0"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}